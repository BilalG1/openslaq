export const routes = {
  channel: (ws: string, id: string) => `/(app)/${ws}/(tabs)/(channels)/${id}` as const,
  dm: (ws: string, id: string) => `/(app)/${ws}/(tabs)/(channels)/dm/${id}` as const,
  thread: (ws: string, id: string) => `/(app)/${ws}/thread/${id}` as const,
  profile: (ws: string, id: string) => `/(app)/${ws}/profile/${id}` as const,
  search: (ws: string) => `/(app)/${ws}/search` as const,
  savedItems: (ws: string) => `/(app)/${ws}/saved-items` as const,
  files: (ws: string) => `/(app)/${ws}/files` as const,
  drafts: (ws: string) => `/(app)/${ws}/drafts` as const,
  huddle: (ws: string) => `/(app)/${ws}/huddle` as const,
  huddles: (ws: string) => `/(app)/${ws}/huddles` as const,
  outbox: (ws: string) => `/(app)/${ws}/outbox` as const,
  threads: (ws: string) => `/(app)/${ws}/threads` as const,
  settings: (ws: string) => `/(app)/${ws}/settings` as const,
  workspaceSettings: (ws: string) => `/(app)/${ws}/workspace-settings` as const,
  browse: (ws: string) => `/(app)/${ws}/(tabs)/(channels)/browse` as const,
  notificationSettings: (ws: string) => `/(app)/${ws}/notification-settings` as const,
  preferences: (ws: string) => `/(app)/${ws}/preferences` as const,
  channels: (ws: string) => `/(app)/${ws}/(channels)` as const,
};
